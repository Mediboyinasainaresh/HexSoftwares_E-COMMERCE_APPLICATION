<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ECOMMERCEAPPLICATION" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_variant</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/on_secondary</item>

        <!-- Background/Surface -->
        <item name="android:windowBackground">@color/on_surface</item>
        <item name="colorSurface">@color/on_surface</item>
        <item name="colorOnSurface">@color/surface</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/on_surface</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>